# 创作入口与创作对话页设计验收报告

## 验收对象

本次把原本挤在起始页里的创作流程拆成两个界面，并修掉三个已上报的问题。

| 截图 | 说明 |
| --- | --- |
| `design-qa-assets/creation-launcher.png` | 起始页 `/`，1440 × 780 |
| `design-qa-assets/creation-capability-menu.png` | 起始页 + 圆形能力按钮展开后的能力列表，1440 × 1200 |
| `design-qa-assets/creation-workspace.png` | 创作对话页 `/create`，含长回复与左侧历次对话，1440 × 1000 |
| `design-qa-assets/creation-workspace-empty.png` | 创作对话页首次进入（无历史对话）的真实组件渲染，1440 × 900 |

截图状态：学生演示身份，devicePixelRatio 1，本地演示模式（`MODEL_EXECUTION_ENABLED=false`）。

## 上报问题与修复

1. **能力按钮挤作一团**：原先 5 个能力按钮（UI 创作 / 图案生成 / Vibe Coding / Word 文档 / PPT 演示）并排放在控制条里，窄屏会换行堆叠。现改为控制条最左侧一个 42px 圆形按钮，显示当前能力的图标；点击后在按钮上方展开列表，每项包含图标、名称与适用场景说明，选中即收起。
2. **长文本直接在入口展示**：起始页不再渲染模型回复，只保留需求输入、能力选择、模型与搜索开关。提交后立即跳转创作对话页，长文在专门的阅读区展开。
3. **缺少历次对话入口**：创作对话页左侧为 252px 会话栏，列出历次对话（能力标签、标题、相对时间与记录条数），可切换、新建与删除；起始页底部同时提供「继续上次对话」与「历次对话（N）」两个入口。

## 设计映射

沿用 ArtEdu 现有设计系统与既有页面结构，没有引入新的视觉语言：

- 两栏工作区复用「我的学习」页的侧栏 + 内容结构（`learning-space` 同族布局），侧栏底色 `#f7f7f8`、1px `#e3e3e3` 分隔、14–20px 圆角。
- 长文本阅读区复用既有 `.ai-conversation` / `.ai-markdown` 排版（16px、行高 1.85、标题/列表/表格/代码块），不新增第二套正文样式。
- 能力列表与控制条按钮沿用项目既有的黑色主色 `#111` 与 Phosphor 图标。
- 会话删除使用平台统一的危险操作确认弹窗（`confirmAction`），与「我的学习」删除任务/笔记一致。

## 实现要点

- 新增 `/create` 路由（`admin-console/src/routing.js`），`routing.js` 的 `sectionFromPath` 自动解析。
- 起始页写入选中的需求（`pending` 任务）到本机 IndexedDB 后跳转 `/create?id=<会话 id>`；创作对话页认领该任务并执行真实的模型 / 本地演示调用，因此两个界面都可以安全刷新。
- 对话保存在浏览器 IndexedDB（按账号隔离，含超长对话压缩），不写入服务端；服务端只记录生成任务与审计。临时参考文件走 `creation-storage` 模块，72 小时未活动自动清理。
- 桌面端（≥781px）阅读区内部滚动，会话栏与控制条固定；窄屏下会话栏折叠为横向滚动条。

## 对比与迭代

1. 第一轮截图发现弹出的能力列表会盖住起始页控制条左侧，无法确认按钮形态；改为单独渲染起始页后确认控制条为「圆形按钮 + 能力名称说明 / 回形针 / 模型下拉 / 搜索开关 / 开始创作」，无重叠与溢出。
2. 第一轮还发现无历史数据时阅读区高度塌陷；补上空状态最小高度与桌面端固定高度后重新截图，布局稳定。
3. 第二轮未发现 P0/P1/P2 问题。能力列表 5 项均完整显示，长文本的标题、有序列表、表格、代码块排版正常，状态行无截断。

## 验证边界（重要）

本机没有可用的 PostgreSQL（Docker CLI 未安装），因此**未能执行登录后的端到端流程**。本次验证覆盖：

- `npm run check` 全绿：API TypeScript 类型检查、前端 Vite 生产构建、Sites Worker 测试 6 项、Markdown 安全渲染测试 2 项。
- 两个新界面在真实浏览器（Chrome headless）中按真实组件渲染，含空状态与有数据两种形态。

尚未覆盖（需在具备数据库的环境补验）：起始页提交 → 跳转 → 自动生成 → 长回复落到会话记录的完整链路；真实模型（DeepSeek）调用；多账号下的会话隔离。

## 图像产物（ModelScope 文生图）

接入 ModelScope（魔搭）`Qwen/Qwen-Image` 后，「UI 创作」与「图案生成」会交付真实图片文件，而不是只返回文字方案。

| 截图 | 说明 |
| --- | --- |
| `design-qa-assets/creation-image-artifact.png` | 回复中内联展示生成的图片 + 文件名 + 下载原图，1440 × 960 |
| `design-qa-assets/modelscope-qwen-image-sample.png` | ModelScope 接口直接产出的样张，760 × 1280（未指定 size 时的默认竖图） |

迭代记录：

1. 首轮发现提示词未指定 `size` 时 Qwen-Image 返回 **760×1280 竖图**，按卡片宽度展开约 707px 高，把回复区撑出滚动条并把「下载原图」推到折叠线下。适配器因此默认请求 `1024x1024` 方图（图标 / 纹样 / UI 本就更适合方构图），并可用 `providerOptions.size` 覆盖。
2. 展示尺寸从 420px 收到 340px、高度用 `min(44vh, 340px)` 封顶；用探针实测渲染盒为 338×338，方图无变形、无裁切。
3. 阅读区在收到新回复后会自动滚到底部，因此图片与其下载入口始终落在视野内。

final result: passed（构建、类型检查、测试与界面渲染通过；端到端流程待数据库环境补验）
