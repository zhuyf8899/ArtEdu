# 2026-09-19 登录环境恢复

## 事实与恢复边界

- 检查时前端 4273、API 4100 均没有监听。Docker 正常，但只存在 `artedu-postgres`，原 `artedu-case-qa-20260914` 不在当前引擎中；不能从此推断是谁删除或重置了容器。
- 现有 `artedu-postgres` 未被覆盖或迁移，原备份未被修改。
- 从本机 2026-09-19 03:02:20 UTC（北京时间 11:02）的配套备份恢复到新容器 `artedu-case-restored-20260919`、独立命名卷及 `apps/api/data/case-restored-20260919-uploads`。
- 数据库及素材归档 SHA-256 与原 verification.json 一致；恢复后 11 作品、55 素材，55 素材哈希均匹配。九个收集案例已发布。备份时点之后的新增数据无法据此确认。
- 恢复后显式执行迁移；不执行 seed，不重置密码。模型执行和 RAG 保持关闭。

## 启动入口

先启动 Docker Desktop，然后在仓库根目录执行：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/start-case-local.ps1 -Container artedu-case-restored-20260919 -UploadRoot ".\apps\api\data\case-restored-20260919-uploads"
```

以上容器名和素材路径是本次本机恢复目标，其他协作者需传入自己的已有案例容器及配套素材路径；仓库不包含数据库、素材或凭据。只在已备份、审阅待执行迁移后添加 `-Migrate`。普通重启不自动迁移。

- 新入口只接受已存在的数据库容器和素材目录；缺失时拒绝启动，不创建空库冒充恢复成功。
- API 固定 4100，前端固定 4273 并显式代理至 4100，绑定本机回环地址。
- 两个服务由独立隐藏后台进程托管，日志在忽略目录 `apps/api/data/case-runtime-*.log`；看到 `READY` 才代表就绪检查通过。
- 已占用端口会拒绝重复启动，不按端口杀进程。若子服务退出，会结束本启动器创建的配对服务并记录错误，避免只剩空壳前端。
- 数据库使用持久卷及 `unless-stopped`。本轮没有配置 Windows 开机自启；电脑重启后仍需启动 Docker 并执行上述命令。
- 密码在运行时从本机 Docker 配置读取，不写入版本库、日志或命令参数。

## 已完成验证

- Docker、API 存活、数据库/素材就绪、前端页面、前端代理均通过。
- 四测试账号：实际登录、读取当前会话、九案例列表均通过，接口验证会话随后注销。
- 浏览器 teacher.demo 重新登录、刷新保持登录、案例详情和展开提示词通过。
- 后端 81 项测试通过；启动脚本语法检查通过；重复启动拒绝且原服务仍健康。
- 未执行模型供应商调用、全站 UI 回归或生产发布。Git 合并不等于生产环境部署，也不会上传本机案例数据库与素材。
- 提交前补充启动器保护测试并纳入 `npm run check`：121 项测试、类型检查及前端构建全部通过；保留既有 Vite 大包告警。最终 GitHub CI 结果以合并请求记录为准。

## 后续建议（未实现）

1. 登录保留安全的站内来源地址，区分会话过期和服务不可用。
2. 社区筛选/排序/数量，真实作品与验收样例分组，补齐封面及可读标题。
3. 案例目录、复制提示词、带入创作（仅预填，不自动消费额度），语义化图片说明。
4. 首页模式入口显式化，精简技术文案，统一辅助字号并补键盘/缩放测试。
