# 2026-09-19 原案例平台恢复与回归

## 本轮完成

1. 核实 Docker 未运行，仅备份白名单内的空运行时套接字并启动 Desktop；没有清空容器、卷或重置账号。
2. 原案例 API 停止期间，对 `artedu-case-qa-20260914` 数据库和对应素材目录做配套备份，保存 SHA-256；验证数据库归档目录及素材 tar 可读。备份位于本地已忽略的 `apps/api/data/backups/`，不随 Git 发布。这不是完整灾难恢复演练。
3. 核实 `rag_chunks` 为零行后，快进本地 main 到 `ee246d3`，在 `feat/case-platform-recovery` 继续修改，并通过原环境启动器应用迁移。没有重新执行 seed 或测试账号初始化。
4. 修复案例视频附件三列网格把文件名挤成竖排的问题：标题、视频、下载操作分行，视频占满可用宽度，文件名可换行；原下载权限判断保持不变。

## 验证结果

- `npm run check`：81 项后端、30 项前端、4 项导入、3 项部署工具测试，共 **118 项**通过，无跳过；类型检查和前端构建通过。保留既有 Vite 主包大小提示。
- 九个收集案例均为已发布，学生账号社区列表和详情可读取；54 个素材哈希与导入清单一致。没有重复导入或再次审批。
- 整个原案例库共 11 个作品、55 个素材（包含原有演示/验收作品），本轮不删除这些记录。
- 4273 前端、4100 API、数据库/素材就绪检查、前端 API 代理与 Docker 均正常。
- 浏览器学生登录通过，ESSENZA 视频约 119.42 秒，可实际播放且无媒体错误；390px、680px 下附件卡片无横向溢出，文件名正常显示。验收后恢复默认窗口尺寸。

## 环境与后续

- 九个案例的本机入口为 `http://localhost:4273/community`；不要把不含这些数据的 4373 演示环境当成同一数据库。
- 原案例环境继续关闭外部模型执行和 RAG。本轮没有把旧环境密钥复制进此环境，没有付费模型实测，也没有部署正式服务器。
- 下一阶段先确认真实模型回归所用环境及供应商配置，再测试对话、图片、Word/PPT/PDF、网页预览的端到端链路。现有自动化测试不代表这些供应商均可用。
- RAG 后续仍需实现 PDF 摄取/切块、embedding 调用、持久化与课程权限过滤下的向量召回，再进行质量验收；仅 embedding 服务接线不等于知识库问答完成。
- 正式发布需要确认目标服务器、维护窗口、独立生产账号和凭据配置，完成 API/前端/Nginx 配套发布及恢复演练。不得把本机测试账号直接开放到公网。
