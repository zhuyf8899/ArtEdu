# 数字皮影实验室入口（非嵌入版）

## 范围

- 设计工作台上方增加独立工具卡片：用途说明、外部工具标识、数字皮影/素材与模板/插件教学标签。
- 普通 HTTPS 外链指向 `https://piying.woooostudio.com/`，使用 `_blank`、`noopener noreferrer` 和 `no-referrer`；不附带账号、令牌、对话等参数。
- 原生 details 展示使用说明：目标站登录和收费以其提示为准；本期不做账号、作品、额度同步。
- 保留原节点工作流；不嵌入 iframe、不代理外站、不修改数据库或模型接口、不自动下载或安装插件。
- 延续白底与浅暖色，使用现有图标库；未下载外部作品图片或制造未经授权的品牌素材。

## 验证

- `npm run check`：124 项测试、类型检查、前端构建通过；已有 Vite 大包提示仍在。
- 新增 3 项测试，验证真实组件渲染后的外链属性、说明文案、默认折叠和工作流入口保留。
- 浏览器教师账号进入设计工作台；Enter/空格展开和收起说明通过；点击外链后 ArtEdu 仍停留在 `/studio`。
- 390px 与 1280px 下无页面横向溢出，入口按钮高度约 48px；验收后恢复浏览器默认尺寸。
- 原“案例分析工作流”节点画布打开和返回通过，未启动工作流或产生模型调用。
- 外部网站的登录、生成、收费、下载等不属于本轮验收范围，不承诺第三方站点持续可用。

## 本地环境说明

验收时 Docker 未运行，启动后报告旧运行时套接字异常；使用项目白名单脚本备份这些空套接字后恢复。启动当前引擎内已有的 `artedu-case-qa-20260914` 和配套 `case-qa-uploads`（55 文件），未恢复备份、重写数据或运行迁移。入口本身没有新增服务依赖。

开发分支为 `feat/piying-tool-entry`；发布与 CI 状态以对应 GitHub 合并请求为准。合并代码不等于生产部署，外部站点不由 ArtEdu 托管。
