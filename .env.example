# 仅供本地开发使用；不要将真实数据库账号或密码提交到 Git。
DATABASE_URL=postgresql://artedu:artedu_dev_only@localhost:5432/artedu
# 当本机已有 PostgreSQL 占用 5432 时，可在根目录 .env 中改为 5433，
# 并同步修改 apps/api/.env 的 DATABASE_URL。
ARTEDU_POSTGRES_PORT=5432
